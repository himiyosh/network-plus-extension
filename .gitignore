node_modules/
coverage/
dist/
*.log
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Playwright MCP
.playwright-mcp/

# Shared rules (local reference only)
docs/unified-project-rules.md

# Environment / config
.env
*.local.json

# Logs / temp
/logs/
/tmp/
.impeccable/hook.cache.json

# Temporary / one-off scripts
fix*.js
test_output.*
test.svg
*.tmp

# Codex-platform mirror of CLAUDE.md and .claude/skills/, generated locally.
# Not committed: it is a blind .claude -> .Codex path substitution written into
# .agents/, so every rules path it names is dangling, and keeping a second copy
# of the same rules only lets the two drift.
AGENTS.md
.agents/
